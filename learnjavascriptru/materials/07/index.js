application.com -> redirect facebook (app_id, info)
                -> ask user ->
                -> redirect application.com (code)

      -> get access_token (code, app_secret) -> access_token, refresh_token
      -> get info (access_token)

  -> auth
  Welcome
